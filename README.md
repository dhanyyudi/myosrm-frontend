# OSRM Inspector Frontend

A modern, glassmorphism-styled web interface for OSRM (Open Source Routing Machine) - built with vanilla JavaScript and MapLibre GL.

## Features

- 🗺️ **Interactive Map** - Powered by MapLibre GL with CartoDB basemaps
- 🚛 **Vehicle Profiles** - Support for trucks, vans, buses with custom routing
- ⏰ **Time-Dependent Routing** - Traffic-aware routing with historical speed data
- 📍 **Multi-Waypoint** - Drag-and-drop waypoint management
- 🎨 **Glassmorphism UI** - Modern translucent interface
- 📊 **Debug Tools** - Visualize nodes, edges, and speed data
- 📤 **Export** - Save waypoints and way IDs as CSV

## Quick Start

```bash
# Serve with any static server
python3 -m http.server 8080

# Or with nginx
nginx -c /path/to/nginx.conf
```

Open http://localhost:8080

## Configuration

The frontend expects an OSRM backend at `/api`. Update `config.js` to change:

```javascript
osrmBackendUrl: "/api",  // Change to your OSRM endpoint
```

## Basemap Options

Edit `map.js` to switch basemaps:

```javascript
// CartoDB Voyager (default) - colorful, modern
const baseMapUrl = "https://basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png";

// CartoDB Positron - light gray, minimal
const baseMapUrl = "https://basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png";

// CartoDB Dark - dark theme
const baseMapUrl = "https://basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png";
```

All basemaps are **free** and use **OSM data**.

## File Structure

```
myosrm-frontend/
├── index.html              # Main HTML
├── app.js                  # Application entry
├── config.js               # Configuration (colors, API URL)
├── map.js                  # MapLibre map initialization
├── routing.js              # OSRM API communication
├── debug.js                # Debug visualization tools
├── routing-area.js         # Service area functionality
├── utils.js                # Utility functions
├── sweetalert-helpers.js   # Alert dialogs
├── styles.css              # Glassmorphism theme
└── runtime-config.json     # Auto-generated by backend
```

## Browser Support

- Chrome/Edge (latest)
- Firefox (latest)
- Safari (latest)

## License

Internal use for SWAT Team
